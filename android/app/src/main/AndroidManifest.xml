<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application
        android:label="MACRO"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        <!-- Authentication Activities -->
        <activity
            android:name=".presentation.authentication.LoginActivity"
            android:exported="false"
            android:theme="@style/LaunchTheme" />
        
        <!-- Onboarding Activities -->
        <activity
            android:name=".presentation.onboarding.ProfileSetupActivity"
            android:exported="false"
            android:theme="@style/LaunchTheme" />
        <activity
            android:name=".presentation.onboarding.WelcomeActivity"
            android:exported="false"
            android:theme="@style/LaunchTheme" />
        <activity
            android:name=".presentation.onboarding.BMICalculatorActivity"
            android:exported="false"
            android:theme="@style/LaunchTheme" />
        <activity
            android:name=".presentation.onboarding.OnboardingDemoActivity"
            android:exported="false"
            android:theme="@style/LaunchTheme" />
        
        <!-- Main App Activities -->
        <activity
            android:name=".presentation.mainapp.MainAppActivity"
            android:exported="false"
            android:theme="@style/LaunchTheme" />
        
        <!-- Settings Activities -->
        <activity
            android:name=".presentation.settings.SettingsActivity"
            android:exported="false"
            android:theme="@style/LaunchTheme" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>